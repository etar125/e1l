- func: split string
- func: substring
- func: mjoin - multiple joins at once
- macros.h (llen from n1md, ARG from suckless, some const,
https://github.com/ramdeoshubham/macros, debug: log, err, etc; ...)
- func: zarr_join
- func: zarr_insert_zarr
- funcs: sarr_tozarr, zarr_tosarr
- one file per function
- write documentation
- rewrite tests
- zarr: normalize string before adding/inserting in zarr
- sarr: normalize string before adding/inserting in sarr
- arr: implement
    struct arr { void **elems; size_t count };
    int  arr_init   (arr*);
    int  arr_add    (arr*, void*);
    int  arr_insert (arr*, size_t, void*);
    int  arr_remove (arr*, size_t);
    void arr_free   (arr*);
